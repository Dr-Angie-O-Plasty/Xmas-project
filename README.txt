
# Enquête — Mini-site (fichiers statiques)

Contenu :
- index.html
- styles.css
- script.js

### Personnalisation rapide
- Ouvre `script.js` et modifie la variable `FINAL_PHONE` (ligne haute) pour mettre le numéro que tu veux que le joueur appelle.
- Tu peux aussi modifier les réponses correctes dans l'objet `CORRECT` (script.js) si tu veux d'autres codes.

### Hébergement
- Option simple et gratuite : GitHub Pages
  1. Crée un dépôt GitHub et mets ces fichiers à la racine.
  2. Active GitHub Pages (Settings -> Pages) et choisis `main` branch / root.
  3. Ton site sera disponible en `https://<ton-user>.github.io/<repo>/`.

- Option alternative : Netlify ou Vercel (drag & drop du dossier `escape_site`).

### Avertissement
- N'inclue pas de numéro de téléphone privé sans le consentement de la personne. Pour un faux répondeur, utilise un numéro virtuel que tu loues (voir les services VoIP).

Amuse-toi 😊
